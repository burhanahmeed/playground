services:
  - type: web
    name: time-playground
    env: node
    buildCommand: npm run build
    startCommand: npm start
    # For static site deployment, use this instead:
    # publishPath: ./build/client
